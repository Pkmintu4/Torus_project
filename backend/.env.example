MONGO_URI=mongodb://<user>:<pass>@host:port/dbname
PORT=5000
JWT_SECRET=your_jwt_secret_here
EMAIL_USER=you@example.com
EMAIL_APP_PASSWORD=your_email_app_password_here
PYTHON=python3

# Rename this file to .env and fill in real values. Do NOT commit real credentials.# Backend service
PORT=5000
NODE_ENV=development
JWT_SECRET=replace_with_strong_random_secret

# Gmail SMTP (use a Gmail App Password, not account password)
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_APP_PASSWORD=your_16_char_app_password
